# cloudflared runs in this namespace and reaches this Service by name.
apiVersion: v1
kind: Service
metadata:
  name: romm
  namespace: romm
spec:
  type: ClusterIP
  selector:
    app: romm
  ports:
    - name: http
      port: 8080
      targetPort: 8080
